stack:
  services:
    web:
      image:
        tag: sha-47e23be
      replicaCount: 1
      resources:
        requests:
          cpu: "250m"
          memory: "512Mi"
        limits:
          cpu: "1000m"
          memory: "2Gi"
    postgres:
      persistence:
        pvc:
          resources:
            requests:
              storage: 10Gi
